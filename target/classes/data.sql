
-- Better to avoid USERS as table name; use APP_USERS
CREATE TABLE IF NOT EXISTS APP_USERS (
                                         ID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                                         FIRST_NAME VARCHAR(100) NOT NULL,
    LAST_NAME  VARCHAR(100) NOT NULL,
    EMAIL      VARCHAR(255) NOT NULL UNIQUE,
    PHONE      VARCHAR(25),
    CREATED_AT TIMESTAMP,
    UPDATED_AT TIMESTAMP
    );

INSERT INTO app_users (id, first_name, last_name, email, phone, created_at, updated_at)
VALUES
    (1, 'Ravi', 'Kumar', 'ravi.kumar@example.com', '9990001111', CURRENT_TIMESTAMP(), CURRENT_TIMESTAMP()),
    (2, 'Anita', 'Sharma', 'anita.sharma@example.com', '9990002222', CURRENT_TIMESTAMP(), CURRENT_TIMESTAMP());